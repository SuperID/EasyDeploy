{
  "name": "rollback",
  "description": "回滚到上一个版本",
  "list": "git reset --hard\r\ngit checkout {{project.branch}}\r\ngit pull\r\ngit reset --hard HEAD~1\r\nnpm install\r\npm2 stop {{project.name}} -f\r\npm2 start {{project.launch}} --name {{project.name}} -x\r\npm2 logs {{project.name}}"
}